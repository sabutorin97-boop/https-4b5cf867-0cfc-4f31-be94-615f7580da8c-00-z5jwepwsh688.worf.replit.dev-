<?php
/**
 * Образец настроек приёма заявок.
 *
 * На сервере скопируйте этот файл рядом, под именем config.php,
 * и впишите настоящие данные:
 *
 *     cp config.sample.php config.php
 *     nano config.php
 *
 * config.php намеренно не хранится в репозитории — в нём пароль.
 * Если файла нет, сайт всё равно работает: заявки записываются в журнал,
 * просто письма не отправляются.
 */

return [
    // Куда приходят заявки
    'recipient' => 'potolok-45@yandex.ru',

    // Где хранить журнал заявок. Каталог должен быть ВНЕ папки сайта,
    // иначе заявки можно будет скачать из браузера.
    'leads_dir' => '/var/www/okna-leads',

    /**
     * Отправка письма через SMTP.
     *
     * Для Яндекса нужен пароль приложения, а не обычный пароль от почты:
     * id.yandex.ru → Безопасность → Пароли приложений → Почта.
     *
     * Поле 'user' и 'from' должны совпадать — Яндекс не даёт отправлять
     * письма от чужого имени.
     *
     * Пока 'enabled' => false, письма не отправляются, но заявки
     * продолжают записываться в журнал.
     */
    'smtp' => [
        'enabled'  => false,
        'host'     => 'smtp.yandex.ru',
        'port'     => 465,
        'user'     => 'potolok-45@yandex.ru',
        'password' => 'сюда-пароль-приложения',
        'from'     => 'potolok-45@yandex.ru',
        'from_name' => 'Сайт «Окна Профигрупп»',
    ],
];
